import React from "react";
import { View, Text, SafeAreaView } from "react-native";

const Chat = () => {
  return (
    <SafeAreaView>
      <Text>Chat</Text>
    </SafeAreaView>
  );
};

export default Chat;
